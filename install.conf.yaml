- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc

- create:
    - ~/Work
    - ~/downloads
    - ~/.vim/undo-history

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
